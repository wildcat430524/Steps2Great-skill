> 📄 **只读镜像**（Steps2Great-skill ← StepsToGreat，由 `_build/sync-from-upstream.mjs` 生成；改内容请改上游再同步）。
> 路径口径：`我的学习/`、`资料/`、`学科/`（= `我的学习/学科/`）指**学生工作区**；`协议/`、`学科包/`、`模板/` 指本 skill 的 `references/` 下同名目录。

# 00_摸底测试（Placement Test）

> 本文件记录**摸底过程与结论**。只增不改 —— 每次重测追加一节，保留历史。
> 剧本（怎么做）→ `协议/01_摸底剧本.md（在 skill 的 references/ 下）`

---

## 第 1 次摸底 · `<yyyy-mm-dd>`

### 前置信息

| 问题 | 学生回答 |
|---|---|
| 为什么学这个 | `<填>` |
| 以前接触过吗 | `<填>` |
| 目标是什么 | `<填>` |

### 探针题与作答

| 层 | 题目 | 学生回答 | 观察 |
|---|---|---|---|
| L1 基础 | `<题>` | `<答>` | `<会/不会>` |
| L1 基础 | `<题>` | `<答>` | |
| L2 应用 | `<题>` | `<答>` | |
| L2 应用 | `<题>` | `<答>` | |
| L3 进阶 | `<题>` | `<答>` | |

### 薄弱点定位

- **崩在哪一层**：`<L1 / L2 / L3>`
- **是概念不清还是不会应用**：`<填>`
- **发现的错误习惯**：`<填，以后重点盯>`

### 起点结论

```
✅ 摸底结论：起点是 <层级>。
   - 已具备：<列出确实会的>
   - 需要补：<最关键的 2–3 个缺口>
   - 建议从：<第几课 / 哪个范围> 开始
   - 预计 <N> 课能到 <目标>（不确定就说不确定）
```

### 学生是否认同结论

`<认同 / 不认同（写明分歧，以学生为准）>`

---

## 第 2 次摸底 · `<yyyy-mm-dd>`（如有重测）

（同上结构）
